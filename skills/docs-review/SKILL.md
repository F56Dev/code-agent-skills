---
name: docs-review
description: >-
  任何项目产出意图/事实类文档时必用——产出后自动触发审查（产出即审纪律）。适用文档：
  spec、plan、宪法/架构文档、阶段报告、能力边界记录、BUG-LEDGER、测试报告、提交前要对齐的文档。
  流程：产出 → 委派 docs-review 子代理（GLM-5.2 异族模型）交叉核对文档 vs 代码真相 → 主 agent
  三级裁决 → 争议回环商讨 → 一致性文档交人工。触发词：产出文档、写完、生成报告、写 spec、
  写 plan、审查文档、review spec、review plan、核对文档、文档与代码一致、doc drift、
  提交前、文档审查、记录 BUG、阶段报告。
---

# Docs Review — 文档审查委派（产出即审 + 商讨回环）

主开发模型写出的文档存在盲区（同族模型思维惯性）。本 skill 规定：**任何项目产出的意图/事实类文档，产出后必须 review**，且 review 是"反馈回路"——产出 → 审查 → 主 agent 思考采纳/否决 → 争议回环商讨 → 一致性文档交人工。

## 触发规则（产出即审）

**默认必审，无豁免判断空间。** 只要写完了**任何一份文档**（声称完成/提交前），默认必须 review，除非它明确命中下面的白名单。

- **必审（默认）**：spec、plan、宪法文档（PRD/architecture/detailed-design）、阶段报告、能力边界记录、BUG-LEDGER、测试报告、提交前要对齐的文档。**以及所有"过程文档"**——Sprint 计划、过程记录、中间方案、任务说明等**只要包含意图/事实/状态/决策，一律必审**。"这只是过程文档"不是免审理由；任何记录了"要做什么、做了什么、当前什么状态、为什么这么决策"的文档都算意图/事实类。
- **白名单（唯一可跳过）**：`temp/` 临时脚本、POC 一次性测试、`__svg_output__/` 一次性 SVG、`tmp` 文件。**主 agent 不得自行扩大白名单**。不确定是否在白名单内 → 按必审处理。
- **时机**：文档"写完了"（声称完成/提交前）触发一次。每份文档完成版审一次，中间编辑版不审（防疲劳）。只有重大返工才需要二轮 review。
- **常态化**：plan 完成后、文档更新后、每 N 次 commit 后，主动检查是否有未审文档。
- **硬性检查**：声称完成任何文档、或准备提交前，**必须自问"这份文档审了吗？"**——未审不能声称完成/提交。这是纪律，不是可选项。

## 委派步骤

1. 调用 `task` 工具（`subagent_type: "docs-review"`）：
   - `description`: 简短语（如 "review sprint plan"）
   - `prompt`: 待审文档**绝对路径** + **聚焦的审查清单（2-4 项，禁止"全面审查"）** + 项目根目录
2. **范围控制（关键，防止 subagent 空返回）**：
   - **禁止**写"全面审查 / 全部核对 / 逐条验证"这类开放范围——subagent 会无限读代码、跑长程、超限静默失败返回空。
   - **必须**让主 agent 先自己想清楚"这份文档最容易出错的是什么"，提炼成 2-4 个聚焦问题交给 subagent 查证。
   - 涉及"核对测试数/模块路径"这类事实时，直接给出预期值（如"文档声称 1531 测试，请核实"），让 subagent 只查证这一条，而不是自己漫游。
   - 若发现 subagent 空返回，重新委派时把范围切得更小（一次只查 1-2 个点）。
3. `docs-review` 返回结构化审查报告（发现按严重度分级，每条带 `文件:行号` 证据）。

## 三级裁决 + 商讨回环（核心协议）

拿到审查报告后，**主 agent 对每条发现分类**：

| 分类 | 含义 | 处理 |
|------|------|------|
| **① 采纳** | 发现成立 | 修改文档 |
| **② 否决-无争议** | 明显错误/无关/与任务无关的小问题 | 直接驳回，**不进商讨** |
| **③ 否决-有争议** | 物理公式 / 架构决策 / 核心事实（影响正确性或方向） | 进入商讨回环 |

**商讨回环（仅 ③ 进入）**：
```
R2: 把主 agent 的否决 + 反驳证据 送回 docs-review
    reviewer 看到新证据后：认输（撤回）或 用更强证据再断言（坚持）
R3: 仍未一致的 → 升到人工（两边立场 + 证据并排）
```

**守卫**：
- **证据门控**：每轮发言必须带 `文件:行号` 或精确推导。**无新证据的坚持直接作废**（拦住耍赖式反驳 + reviewer 幻觉式断言）。
- **轮次上限**：最多 R1→R2→R3。R3 不一致不硬压，直接升人类。
- **分级商讨**：只有 ③ 进回环，控制延迟与成本。

**完成后**：
- 一致性文档（含未决争议清单）→ 交人工最终 review
- 人类是最终裁决者：商讨结果仍是候选，人工可推翻

## 示例

```
task(
  subagent_type: "docs-review",
  description: "verify plan test count",
  prompt: "请审查 D:\\my code\\phi_emu\\docs\\superpowers\\2026-08-01-PHASE10-SPRINT-B.md 的**一个点**：
  文档第 5 行声称基线 1461 测试 / 134 文件。请只核实这一条：
  在 D:\\my code\\phi_emu 下数 src/**/*.test.ts 文件数和 npm test 输出的测试数，报告实际值。
  项目根目录 D:\\my code\\phi_emu。不要审查其他内容。"
)

# 聚焦版（2-4 项清单，替代"全面审查"）
task(
  subagent_type: "docs-review",
  description: "review phase10 plan (focused)",
  prompt: "请审查 D:\\my code\\phi_emu\\docs\\superpowers\\2026-08-01-PHASE10-SPRINT-B.md，只查以下 3 点：
  1) 文档声称 env.G 默认 0——请 grep src/physics/domain-types.ts 确认
  2) 文档说 contributeEquations 签名含 connections? 参数——读 domain-types.ts 确认
  3) 第 3 节是否有自相矛盾（冲刺归属）
  项目根目录 D:\\my code\\phi_emu，宪法文档在 docs\\superpowers\\。只查这 3 点，不要扩大范围。"
)
```

## 注意

- review 定位是**意图层**（文档对不对）早期预警；测试负责**实现层**（代码对不对）。两者互补，不替代。
- 子代理只审查不修改（edit/write 已禁）。发现的问题回到主流程修。
- 若发现子代理幻觉（无依据断言），在回环里点出并附证据，它下次更严格。
